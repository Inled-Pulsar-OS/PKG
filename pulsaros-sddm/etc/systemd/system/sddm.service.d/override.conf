[Unit]
Conflicts=plymouth-quit-wait.service
After=plymouth-start.service

[Service]
# Start plymouthd immediately when SDDM stops to capture the framebuffer before the graphics console resets
ExecStopPost=-/usr/sbin/plymouthd --mode=reboot --attach-to-session
ExecStopPost=-/usr/bin/plymouth show-splash

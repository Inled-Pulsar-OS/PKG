# Maintainer: Jaime <info@inled.es>
pkgname=pulsaros-cloud
pkgver=1.0.0
pkgrel=5
pkgdesc="Cloud drives for the Pulsar OS Finder: any provider supported by rclone"
url="https://inled.es"
license=(GPL-3.0-or-later)
arch=(x86_64)
depends=(
  fuse3
  gnome-terminal
  python3
  rclone
)
source=(pulsar-cloud
        "pulsar-cloud@.service"
        onedrive.svg
        google-drive.svg
        pulsar-cloud-symbolic.svg)
sha256sums=(SKIP SKIP SKIP SKIP SKIP)

package() {
  install -Dm755 "${srcdir}/pulsar-cloud" "${pkgdir}/usr/bin/pulsar-cloud"
  install -Dm644 "${srcdir}/pulsar-cloud@.service" \
    "${pkgdir}/usr/lib/systemd/user/pulsar-cloud@.service"
  install -Dm644 "${srcdir}/onedrive.svg" \
    "${pkgdir}/usr/share/icons/hicolor/scalable/apps/onedrive.svg"
  install -Dm644 "${srcdir}/google-drive.svg" \
    "${pkgdir}/usr/share/icons/hicolor/scalable/apps/google-drive.svg"
  install -Dm644 "${srcdir}/pulsar-cloud-symbolic.svg" \
    "${pkgdir}/usr/share/icons/hicolor/symbolic/apps/pulsar-cloud-symbolic.svg"
}

# Maintainer: Pulsar OS Team <team@pulsaros.io>
pkgname=tubeos-plymouth
pkgver=0.1.0
pkgrel=1
pkgdesc="Tube OS Plymouth boot splash theme"
arch=('any')
url="https://github.com/Inled-Pulsar-OS"
license=('Apache-2.0')
depends=('plymouth')
source=()
sha256sums=()

package() {
  local _src="$(cd "$startdir"/../../../tubeos-plymouth && pwd)"
  if [ -d "$_src/usr" ]; then
    cp -r "$_src/usr" "$pkgdir/"
  fi
  if [ -d "$_src/etc" ]; then
    cp -r "$_src/etc" "$pkgdir/"
  fi
}
